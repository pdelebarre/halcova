TEAM: OFFLINE
CURRENT ISSUE: #159 M2 Offline Capability Matrix & Collector UX
STATUS: ACTIVE — Security Auditor HOLD (fail-open privacy reset) remediated on #159 (commit 0e45f33). clearOutboxForUser + clearMirrorForUser now FAIL CLOSED: resolve false on any IndexedDB delete failure (abort/quota/cursor/open) instead of `catch { return true }`, and resolve true only on tx.oncomplete (commit), so a raw queued op (pendingItem/barcode/ocrText) or private record can never survive a reported 'cleared'. SettingsModal.handleClearOfflineData is atomic for the reset outcome: if either clear fails it surfaces a safe generic error (ADR-0019 Dec 12, no secrets/raw content) + retry trigger, never 'Offline data cleared'. Read interface still returns only safe {opId,status,kind}; cross-user isolation test still green (u2 untouched).
ACTIVE PR: #422 (m2/offline/159) — MERGEABLE/conflict-free; BLOCKED only on pending independent re-verification
LAST GATE: local PASS — affected suites green (settings-modal incl. outbox-clear isolation + cancel-focus, offlineOutbox incl. markFailed->ERROR, outbox, outboxSync, useOutboxSync, sync-status, no-silent-fallback, offlineMirror, useCollection, collection-view-*, App, i18n) + NEW fail-closed negative tests (outbox/mirror clear returns false + op/records durable; settings-modal failure-path UI shows safe error, not 'cleared'); full src suite 1151 passed (112 files); oxlint clean on touched files.
BLOCKER: none
NEXT: independent Security Auditor + Ergonomics Reviewer + Tester re-verification (not self-approved) on commit 0e45f33
