TEAM: OFFLINE
CURRENT ISSUE: #159 M2 Offline Capability Matrix & Collector UX
STATUS: ACTIVE — rebased onto main incl. merged #292 outbox (outbox.js/outboxSync.js/useOutboxSync.js). offlineOutbox read interface wired to the REAL #292 durable outbox (surfaces only safe {opId,status,kind}; failed->error, pending->pending; conflict stays 0 until M3). CollectionView Sync-now bound to useCollection.flushOutbox (push+reconcile, NOT re-pull). useCollection.mutationSeq bumps after add/update/remove so the SyncStatus strip re-reads the real queue (reachable states). SyncStatus surfaces "All changes synced" after a drain; Sync-now target >=44px. SettingsModal clear flow manages focus (confirm button + role="status" done line). Docs/offline-capability-matrix.md updated to the wired reality (add queued in M2; edit/delete + conflict M3). no-silent-fallback test updated to merged #292 semantics (safe failure stages a TRACKED op; auth failure re-throws/stages nothing).
ACTIVE PR: #422 (m2/offline/159) — rebased, conflict-free
LAST GATE: local PASS — affected suites 141 passed (offlineOutbox, sync-status, settings-modal, no-silent-fallback, useCollection-offline, useOutboxSync, outbox, outboxSync, useCollection, offlineMirror, collection-view-offline-copy, collection-view-refresh, i18n); full src suite 1141 passed; oxlint no new errors/warnings in touched files.
BLOCKER: none
NEXT: independent Ergonomics Reviewer (offline journey) + Security Auditor + Tester re-verification (not self-approved)
