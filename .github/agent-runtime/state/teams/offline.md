TEAM: OFFLINE
CURRENT ISSUE: #159 M2 Offline Capability Matrix & Collector UX
STATUS: ACTIVE — independent-gate HOLDs remediated on #159 (commit 5a71e9e). ERGONOMICS: useCollection.flushOutbox bumps mutationSeq after a successful push so useOfflineSyncStatus ([online, syncId]) re-reads the outbox and reaches "All changes synced" after a Sync-now drain; CollectionView mounts useOutboxSync for reconnect/foreground auto-flush; SettingsModal cancel restores focus to the clear trigger. SECURITY: SettingsModal 'Clear offline data' now clears the durable #292 outbox (clearOutboxForUser) alongside the mirror (clearMirrorForUser), both user-scoped (mirrorScope), so no raw queued mutation (pendingItem/barcode/ocrText) auto-flushes on reconnect; another user's outbox/mirror untouched. offlineOutbox read interface still returns only safe {opId,status,kind}.
ACTIVE PR: #422 (m2/offline/159) — MERGEABLE/conflict-free; BLOCKED only on pending review (independent gates re-verify)
LAST GATE: local PASS — affected suites 96 passed (settings-modal incl. outbox-clear isolation + cancel-focus, useCollection-offline flush-seq, useOutboxSync, sync-status, no-silent-fallback, offlineOutbox, outbox, outboxSync, useCollection, collection-view-offline-copy, collection-view-refresh, App); full src suite 1143 passed (111 files); oxlint clean on touched files.
BLOCKER: none
NEXT: independent Ergonomics Reviewer + Security Auditor + Tester re-verification (not self-approved) on commit 5a71e9e
