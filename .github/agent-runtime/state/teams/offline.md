TEAM: OFFLINE
CURRENT ISSUE: #158 M2 IndexedDB Local-First Persistence for Collector Workflows
STATUS: PASS — local implementation complete. Introduced `idb` library (v8.0.3), created formal repository layer (`src/repositories/localDatabase.js`) with versioned schema (v1), tombstones, sync-status columns (updatedAt/serverVersion/localVersion/syncStatus), scope-based isolation, and export. Updated `useCollection.js` to persist to the local item repository alongside the existing mirror/outbox. Added export UI to SettingsModal. Updated clear handler to also clear the local item store. All 2665 tests pass (196 files). Coverage: statements 87.97%, branches 80.42%, functions 86.37%, lines 91.13% — all above 70% threshold. New `src/repositories/` modules: localDatabase.js (80.46% stmts, 75.53% branches), mirrorRepository.js (delegation wrapper, tested). Security: no credentials in IDB (tested), cross-user isolation (tested), sign-out clear (tested), fail-closed on all IDB failures.
ACTIVE PR: m2/offline/158 (not yet pushed)
LAST GATE: local PASS — all affected suites green (localDatabase.test.js 29 tests, mirrorRepository.test.js 4 tests, settings-modal-clear-failure 24 tests, i18n 48 tests, offlineMirror 24 tests, outbox 16 tests, itemUuid 6 tests, useCollection 6 tests); full suite 2665 passed (196 files).
BLOCKER: none
NEXT: push branch, create PR, await independent Offline Architect + Security Auditor + Tester re-verification (not self-approved)