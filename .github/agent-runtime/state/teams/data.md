TEAM: DATA
CURRENT ISSUE: #315-fix — deployment-blocking defect: `collection-types.test.js` bundled as Netlify function (`.` violates function-name rule → 422). Moved to `_shared/` per established pattern.
STATUS: IMPLEMENTED on m3/data/315-fix. `netlify/functions/collection-types.test.js` → `netlify/functions/_shared/collection-types.test.js` (git mv + 4 import-path updates: `../collection-types`, `./repositories/test-helpers`, `./postgres`, `./policy`). No application logic changed. Real function `netlify/functions/collection-types.js` untouched.
ACTIVE PR: (pending) m3/data/315-fix — not yet opened; human merge authority retained.
LAST GATE: vitest `netlify/functions/_shared/collection-types.test.js` → 5/5 passed. `netlify/functions/*.test.js` empty (no `.test.js` in deployable dir). No schema/migration/API/tenancy change → no Data Architect / API / Multi-tenant gate triggered.
BLOCKER: none.
NEXT: PM open PR for m3/data/315-fix and route to independent verification (Tester/Release Validator) before merge.
---
COORDINATION NOTE (prior issue #316, still pending): #316 (PR #428, m3/data/316) — MERGEABLE; HOLD A (Security Auditor) + HOLD B (Multi-tenant Security) remediation pushed (RLS 013 canonical_service role + assert_service_identity + assert_clean_public_jsonb; rls-integration.test.js negative/positive cases). Those gates remain OPEN pending re-verdict before production cutover. Full pg-mem suite green (2410 passed / 10 integration skipped). NEXT for #316: PM re-run Security + Multi-tenant gates on #428, then Tester + Release Validator.
---
COORDINATION NOTE (parallel worktree, superseded): #315 (PR #425) + #317 (PR #426) are NOW MERGED into main. The #316 rebase staged ONLY #316 files (migration 012, RLS 013, collection-migration.js(+test), migrate-collections.mjs, rls-migration.test.js) + data.md checkpoint. Prior shared-tree #315/#317 leftovers (registry test edits + untracked provider files) were preserved in `git stash@{0}` during the rebase for safety and are recoverable; two teams must not work the shared checkout concurrently.