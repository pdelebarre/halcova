TEAM: DATA
CURRENT ISSUE: #316 — [FEAT-6.3] Migrate Books & Records to Generic Collections (ADR-0020 §11; epic #313)
STATUS: IMPLEMENTED + HOLD REMEDIATION on m3/data/316 (rebased cleanly onto origin/main; #315/#317/provider coverage merged — no longer stacked). HOLD A (Security Auditor) fixed in db/rls/013: dedicated `canonical_service` role + in-function `assert_service_identity()` gate on `session_user` (unforgeable, non-settable-GUC) + `assert_clean_public_jsonb` payload allowlist/sanitization (object-shape, bounded keys/length, provider-ids key allowlist, stored-XSS char guard) + SERVER-derived row id (never trusts caller p_id; p_id removed from signature) + EXECUTE revoked from app_rls/PUBLIC, granted only to canonical_service. HOLD B (Multi-tenant Security) extended in netlify/functions/_shared/rls-integration.test.js: canonical_items INSERT/UPDATE/DELETE rejected for app_rls, u1 cross-tenant SELECT of u2 collection_items = 0 rows, migrated CollectionItem resolves to owner collection only (IDOR path 0 rows), non-service app_rls cannot invoke canonical_upsert_service, + positive canonical_service gate path (superowner gated). Migration SQL 012 (canonical_items + collection_items FK, pg-mem-safe, idempotent) + RLS 013 (CanonicalItem service-identity write control: SELECT-only policy, app_rls SELECT-only, FORCE RLS, SECURITY DEFINER canonical_upsert_service) + migration TOOL (collection-migration.js backfill/reconcile/rollback + scripts/migrate-collections.mjs) + tests (idempotency, reconciliation zero loss/dup/ownership change, reverse-mapping rollback). Postgres + Blobs paths untouched; legacy items + API contracts preserved until reconciliation PASS + retirement ADR.
ACTIVE PR: #428 (m3/data/316) — MERGEABLE (conflicts resolved; HOLD A + HOLD B remediation pushed; rls-integration harness errors fixed)
LAST GATE: DATA ARCHITECT PASS (data-architecture/migration boundary, #316) + Security-Auditor / Multi-tenant-Security HOLDs A & B remediated (code pushed for re-review; those gates remain OPEN pending their re-verdict). Full pg-mem suite green (2410 passed / 10 integration skipped); rls-integration new negative/positive cases run only under real-PG RLS_INTEGRATION=1. Real-PG rls-integration harness errors fixed: (1) UPDATE/DELETE negative assertions split into separate transactions so an expected privilege abort no longer masks the DELETE with "current transaction is aborted"; (2) canonical_service granted SELECT-only on canonical_items (013) so the positive-path verification SELECT no longer fails "permission denied for table canonical_items" (DML deliberately withheld — SECURITY DEFINER canonical_upsert_service remains sole write surface).
BLOCKER: none for mergeability. PR #428 MERGEABLE (mergeStateStatus BLOCKED only by repo branch-protection/required-review; human retains merge authority). Security-Auditor + Multi-tenant-Security re-review required on the HOLD A/B remediation before any production cutover.
NEXT: PM re-run Security Auditor + Multi-tenant Security gates on #428's updated branch (013 + rls-integration.test.js), then Tester + Release Validator before production cutover; recommend noting canonical_upsert_service now requires the canonical_service role (operator must provision LOGIN/credentials) in the #317 service wiring.
---
COORDINATION NOTE (parallel worktree, superseded): #315 (PR #425) + #317 (PR #426) are NOW MERGED into main. The #316 rebase staged ONLY #316 files (migration 012, RLS 013, collection-migration.js(+test), migrate-collections.mjs, rls-migration.test.js) + data.md checkpoint. Prior shared-tree #315/#317 leftovers (registry test edits + untracked provider files) were preserved in `git stash@{0}` during the rebase for safety and are recoverable; two teams must not work the shared checkout concurrently.