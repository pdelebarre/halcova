TEAM: DATA
CURRENT ISSUE: #165 — [ARCH-6.1] Harden PostgreSQL Schema, Tenancy & Migrations (M3 prerequisite)
STATUS: ACTIVE — HOLD 1/2/3 remediated on m3/data/165 (real-Postgres RLS enforcement wired into blocking security-ci rls-integration job; app_rls sessions grant narrowed to SELECT-only; admin authority moved off forgeable app.admin_session GUC to token-derived assert_admin_session(session_token_hash)); broken rls-integration harness fixed on #424: tenant-context assertions now run inside explicit BEGIN/set_config/COMMIT transactions on a single checked-out client (matching withTenantTransaction) so the GUC is never lost to pool semantics
ACTIVE PR: #424 (m3/data/165)
LAST GATE: DATA (Data Architect, independent) — PASS on #424 (m3/data/165) + HOLD remediation: rls-integration.test.js (RLS_INTEGRATION-gated) proves cross-tenant fail-closed, non-admin cannot invoke admin fns even after forging app.admin_session (HOLD 3), app_rls has no DML on sessions (HOLD 2); security-ci.yml rls-integration job executes it against real Postgres on every PR/merge (HOLD 1, blocking, no continue-on-error); pg-mem green (2311 pass + 4 integration skipped); real-Postgres 16 re-run of npm run db:test:rls: 4/4 integration tests PASS (was 3/4, cross-tenant SELECT/UPDATE/INSERT green); 011 + tenant-rls.js + docs updated; residual (leaked app_rls cred / arbitrary SQL injection) documented as separate control in tenancy-and-rls.md
BLOCKER: Multi-tenant Security + Security Auditor + Tester gates still pending re-review of remediation (independent, not waived by Data Architect)
NEXT: PM coordinate re-review of HOLD 1/2/3 remediation on #424 (incl. rls-integration harness fix); then #315/#316/#317
