# M2 State — Offline-first

> Compact PM state. Canonical: #355 · ADR-0011 · ADR-0019 · ADR-0018.

STATUS: **DORMANT** — gated by M1 security gate (#337/#342).

SCOPE:
- #289 Offline Collection Mirror (IndexedDB)
- #292 Offline Capture Outbox, Offline Add & Reconnect Sync — outbox creation, local op IDs, minimal idempotent push + reconcile
- #159 Offline capability matrix & collector UX
- (M2 does NOT include #160/#161 — full bidirectional pull/cursors/OCC/conflict matrix is M3; ADR-0019 Decision 7/8)

ENTRY CRITERIA:
- M1 security gate PASS (#342) + follow-ups (#376/#378/#399) disposed

TEAMS:
- OFFLINE (lead) · DATA (schema/migrations) · SECURITY (offline trust/tenant)

EXIT CRITERIA:
- Offline mirror + outbox live; sync idempotent; conflict policy recorded; offline trust matrix enforced; Tester + Security Auditor + Offline Architect PASS

RISKS:
- Offline trust/authorization is security-sensitive → Security Auditor mandatory
- ADR-0019 proposed → confirm (Offline + Data + Security re-review) before M2 implementation
- M2 push limited to minimal conflict matrix (ADR-0016 rule 12); no silent mutation discard
