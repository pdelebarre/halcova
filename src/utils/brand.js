export const APP_NAME = 'Halcova'

// Public brand vocabulary. Internal storage keys and legacy environment names
// intentionally remain unchanged for backwards compatibility.
export const BRAND = Object.freeze({
  name: APP_NAME,
  shortName: APP_NAME,
  description: 'Your personal collection, catalogued.',
})
